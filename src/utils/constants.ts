const CANVAS_DIMENSIONS = {
  WIDTH: 640,
  HEIGHT: window.innerHeight,
};

const PLAYER_DIMENSIONS = {
  WIDTH: 124,
  HEIGHT: 120,
};

const PLATFORM_DIMENSIONS = {};

export { CANVAS_DIMENSIONS, PLAYER_DIMENSIONS, PLATFORM_DIMENSIONS };
