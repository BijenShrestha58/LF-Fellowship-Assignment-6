const CANVAS_DIMENSIONS = {
  WIDTH: 640,
  HEIGHT: window.innerHeight,
};

const PLAYER_DIMENSIONS = {
  WIDTH: 92,
  HEIGHT: 92,
};

const PLATFORM_DIMENSIONS = {};

enum GAME_STATE {
  START = "START",
  PLAYING = "PLAYING",
  GAME_OVER = "GAME_OVER",
}

export {
  CANVAS_DIMENSIONS,
  PLAYER_DIMENSIONS,
  PLATFORM_DIMENSIONS,
  GAME_STATE,
};
